{
	"name": "root",
	"private": true,
	"workspaces": [
		"demo",
		"packages/*"
	],
	"scripts": {
		"build": "tsc -b & yarn workspaces foreach run build",
		"check": "yarn workspaces foreach run check",
		"demo": "yarn workspace demo start",
		"format": "prettier --write .",
		"lint": "prettier --check ."
	},
	"devDependencies": {
		"prettier": "^2.6.2",
		"typescript": "~4.7.3"
	},
	"type": "module",
	"packageManager": "yarn@3.2.1"
}
