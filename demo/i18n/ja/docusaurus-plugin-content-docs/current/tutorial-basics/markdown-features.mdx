---
sidebar_position: 4
---

# Markdownの特徴

Docusaurus は<strong><a href="https://daringfireball.net/projects/markdown/syntax">Markdown</a></strong>といくつかの<strong>追加</strong>機能をサポートしています。

## フロントマター

Markdown文書には、<a href="https://jekyllrb.com/docs/front-matter/">Front Matterと</a>呼ばれるメタデータが上部にあります。

```text title="my-doc.md"
// highlight-start
---
id: my-doc-id
title: My document title
description: My document description
slug: /my-custom-url
---
// highlight-end

## Markdown heading

Markdown text with [links](./hello.md)
```

## リンク

通常のMarkdownリンクは、URLパスまたは相対ファイルパスを使用してサポートされています。

```md
Let's see how to [Create a page](/create-a-page).
```

```md
Let's see how to [Create a page](./create-a-page.md).
```

<strong>結果</strong> Let's see how to <a href="./create-a-page.md">Create a page</a>.

\#画像

通常のMarkdown画像に対応しています。

静的ディレクトリにある画像<code>（static/img/docusaurus.png</code>）を参照するために絶対パスを使用することができます。

```md
![Docusaurus logo](/img/docusaurus.png)
```

ドクソーラスロゴ]\(/img/docusaurus.png)

<a href="../tutorial-extras/manage-docs-versions.md">追加ガイドに</a>あるように、現在のファイルからの相対的な画像参照も可能です。

## コードブロック

Markdownのコードブロックは、Syntaxハイライトに対応しています。

```jsx title="src/components/HelloDocusaurus.js"
function HelloDocusaurus() {
	return <h1>Hello, Docusaurus!</h1>;
}
```

```jsx title="src/components/HelloDocusaurus.js"
function HelloDocusaurus() {
	return <h1>Hello, Docusaurus!</h1>;
}
```

## ♪ 戒めよ

ドキュソーには、諭吉や吹き出しを作成するための特殊な構文があります。

:::チップ 私のチップ

この素晴らしい機能オプションを使用する

:::

:::危険 お気をつけて

この行為は危険です

:::

:::チップ 私のチップ

この素晴らしい機能オプションを使用する

:::

:::危険 お気をつけて

この行為は危険です

:::

## MDXとReactコンポーネント

<a href="https://mdxjs.com/">MDX</a> can make your documentation more <strong>interactive</strong> and allows using any <strong>React components inside Markdown</strong>:

```jsx
export const Highlight = ({children, color}) => (
  <span
    style={{
      backgroundColor: color,
      borderRadius: '20px',
      color: '#fff',
      padding: '10px',
      cursor: 'pointer',
    }}
    onClick={() => {
      alert(`You clicked the color ${color} with label ${children}`)
    }}>
    {children}
  </span>
);

This is <Highlight color="#25c2a0">Docusaurus green</Highlight> !

This is <Highlight color="#1877F2">Facebook blue</Highlight> !
```

export const Highlight = ({ children, color }) => (
	<span
		style={{
			backgroundColor: color,
			borderRadius: '20px',
			color: '#fff',
			padding: '10px',
			cursor: 'pointer'
		}}
		onClick={() => {
			alert(`You clicked the color ${color} with label ${children}`);
		}}
	>
		{children}
	</span>
);

これは<Highlight color="#25c2a0">ドキュサウルスグリーン</Highlight>

これが<Highlight color="#1877F2">フェイスブックブルー</Highlight>だ
